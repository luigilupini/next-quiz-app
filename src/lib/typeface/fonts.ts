import { Karla, Rubik } from "next/font/google"

export const karla = Karla({
  subsets: ["latin"],
  variable: "--font-karla",
})

export const rubik = Rubik({
  subsets: ["latin"],
  variable: "--font-rubik",
})
